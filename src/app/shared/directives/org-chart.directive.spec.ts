import { OrgChartDirective } from './org-chart.directive';

describe('OrgChartDirective', () => {
  it('should create an instance', () => {
    const directive = new OrgChartDirective();
    expect(directive).toBeTruthy();
  });
});
